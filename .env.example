# Copy sang .env và điền giá trị thật — .env không được commit (xem .gitignore)

DB_HOST=127.0.0.1
DB_NAME=htsoft_web
DB_USER=root
DB_PASS=

# Zalo Cloud Account (ZCA) — tài khoản Zalo dùng để gửi mật khẩu OTP trực tiếp
ZALO_ZCA_SESSION_PATH=./storage/zca-session.json
ZALO_ZCA_PROXY=
